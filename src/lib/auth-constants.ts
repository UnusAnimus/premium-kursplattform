/** Duration (ms) a password reset token is valid. */
export const PASSWORD_RESET_EXPIRY_MS = 60 * 60 * 1000; // 60 minutes

/** Human-readable label used in UI and email templates. */
export const PASSWORD_RESET_EXPIRY_LABEL = '60 Minuten';
